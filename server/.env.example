# Server environment variables

# Option 1: Store the full JSON content as a string (recommended for Render/Heroku)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Option 2: Place serviceAccount.json file in the 'server/' folder.
# It is already added to .gitignore, so it won't be uploaded to GitHub.

PORT=5173
